<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>

<p>注文番号：<%= @order.number %></p>

<div class="order-form">

  <div class="col-sm-offset-2 col-sm-10">
      <%= link_to "注文画面に戻る", new_order_path, method: :get, class: "btn btn-primary"%>
    <button type="button" class="btn btn-outline-danger" data-toggle="modal" data-target="#deleteModal">
      取り消し
    </button>
  </div>

  <div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="modal" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="demoModalTitle">この注文を取り消しますか？</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">キャンセル</button>
          <%= link_to "取り消す", order_path(@order.id), method: :delete, class: "btn btn-danger" %>
        </div>
      </div>
    </div>
  </div>
</div>
